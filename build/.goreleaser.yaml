# GoReleaser configuration for Ajime Agent
# Note: This is for reference - Rust builds use cargo, not Go

version: 2

project_name: ajigent

before:
  hooks:
    - cargo build --release --target {{ .Target }}

builds:
  - id: ajigent
    binary: ajigent
    builder: prebuilt
    prebuilt:
      path: target/{{ .Target }}/release/ajigent
    goos:
      - linux
    goarch:
      - amd64
      - arm64
      - arm
    goarm:
      - "7"

archives:
  - id: ajigent
    format: tar.gz
    name_template: "{{ .ProjectName }}-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    files:
      - LICENSE
      - config/settings.yaml
      - scripts/install.sh

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  github:
    owner: ajime
    name: ajigent
  draft: true
  prerelease: auto

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
